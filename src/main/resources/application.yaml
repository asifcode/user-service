spring:
  application:
    name: user-service
  datasource:
    url: jdbc:postgresql://localhost:xyz/mydatabase
    username: xyz
    password: xyz
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 20000
      maximum-pool-size: 5
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
#eureka:
  #client:
    #serviceUrl:
      #defaultZone: http://localhost:8080/eureka
server:
  port: 9090